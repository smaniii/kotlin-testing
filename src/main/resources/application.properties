#TODO have this set in the env properties keeping it like this just for development testing
spring.datasource.url=jdbc:mariadb://localhost:3306/docker_db
spring.datasource.username=root
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
jwt.secret=SECRET
auth.header=Auth
token.prefix=Bearer
#time in days
token.expire.time=1